<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RcloneGui.Features.Dashboard.Views.ConnectionTypeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RcloneGui.Features.Dashboard.Views"
    NavigationCacheMode="Disabled">
    
    <ScrollViewer Padding="0,0,16,0">
        <StackPanel Padding="24" MaxWidth="600" HorizontalAlignment="Left">
            <!-- Header -->
            <TextBlock Text="Add Connection" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Select the type of remote storage" 
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Margin="0,4,0,24"/>
            
            <!-- Connection Type Grid -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- SFTP Card -->
                <Button x:Name="SftpCard" 
                        Grid.Column="0" Grid.Row="0"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Padding="16"
                        Margin="0,0,8,8"
                        Click="SftpCard_Click">
                    
<StackPanel HorizontalAlignment="Center">
                        <Grid Width="48" Height="48" 
                              CornerRadius="8"
                              Background="{ThemeResource AccentFillColorDefaultBrush}"
                              HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE8B7;" 
                                      FontSize="24" 
                                      Foreground="White"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="SFTP" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Button>
                
                <!-- Google Drive Card (Disabled) -->
                <Button x:Name="GoogleDriveCard" 
                        Grid.Column="1" Grid.Row="0"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Padding="16"
                        Margin="4,0,4,8"
                        IsEnabled="False">
                    
<StackPanel HorizontalAlignment="Center">
                        <Grid Width="48" Height="48" 
                              CornerRadius="8"
                              Background="{ThemeResource SystemFillColorNeutralBrush}"
                              HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE753;" 
                                      FontSize="24" 
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="Google Drive" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                        
                        <TextBlock Text="Soon" 
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- OneDrive Card (Disabled) -->
                <Button x:Name="OneDriveCard" 
                        Grid.Column="2" Grid.Row="0"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Padding="16"
                        Margin="8,0,0,8"
                        IsEnabled="False">
                    
<StackPanel HorizontalAlignment="Center">
                        <Grid Width="48" Height="48" 
                              CornerRadius="8"
                              Background="{ThemeResource SystemFillColorNeutralBrush}"
                              HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE753;" 
                                      FontSize="24" 
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="OneDrive" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                        
                        <TextBlock Text="Soon" 
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- Dropbox Card (Disabled) -->
                <Button x:Name="DropboxCard" 
                        Grid.Column="0" Grid.Row="1"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Padding="16"
                        Margin="0,8,8,0"
                        IsEnabled="False">
                    
<StackPanel HorizontalAlignment="Center">
                        <Grid Width="48" Height="48" 
                              CornerRadius="8"
                              Background="{ThemeResource SystemFillColorNeutralBrush}"
                              HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE753;" 
                                      FontSize="24" 
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="Dropbox" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                        
                        <TextBlock Text="Soon" 
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- S3 Card (Disabled) -->
                <Button x:Name="S3Card" 
                        Grid.Column="1" Grid.Row="1"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Padding="16"
                        Margin="4,8,4,0"
                        IsEnabled="False">
                    
<StackPanel HorizontalAlignment="Center">
                        <Grid Width="48" Height="48" 
                              CornerRadius="8"
                              Background="{ThemeResource SystemFillColorNeutralBrush}"
                              HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE753;" 
                                      FontSize="24" 
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="Amazon S3" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                        
                        <TextBlock Text="Soon" 
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- FTP Card -->
                <Button x:Name="FtpCard" 
                        Grid.Column="2" Grid.Row="1"
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch"
                        Padding="16"
                        Margin="8,8,0,0"
                        Click="FtpCard_Click">
                    
<StackPanel HorizontalAlignment="Center">
                        <Grid Width="48" Height="48" 
                              CornerRadius="8"
                              Background="{ThemeResource AccentFillColorDefaultBrush}"
                              HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE8B7;" 
                                      FontSize="24" 
                                      Foreground="White"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="FTP" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Button>
            </Grid>
            
            <!-- Info Text -->
            <TextBlock Text="More providers coming soon" 
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       Margin="0,16,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
